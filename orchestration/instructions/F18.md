# F18 — fix ticket (bundle): observability + plugin surface (F15, F17a, F17b, N-F14-1)

Four adjudicated findings, one ticket because they share files. Read `DESIGN.md` first (BINDING, see
RULING F18), then `PRD.md` §6.R7 / §6.R8 / §7 Appendix B, then this card. F16 is already merged on
your base — the write-verify semantics and L1 split it introduced are the current truth.

## Findings being fixed

- **F17b (most important):** the monitor-only/degraded latch is invisible. `afanctl.state.v1` keeps the
  *commanded* mode (`src/supervisor.rs`'s own test asserts `state["mode"] == "curve"` while degraded),
  so during the F16 hardware event the daemon reported `mode: "curve"` while writing nothing. The
  plugin would render "curve" for a daemon doing nothing — a wrong display of a safety-relevant state.
- **F17a:** human `status` prints daemon/sensors/t_eff/fan/config but **not** `mode`, `target_rpm` or
  `recent_errors`, although `README.md` promises all three.
- **F15:** non-root `afanctl status` prints a scary `WARN afanctl::smc: cannot pre-open fan1_manual
  O_WRONLY; L2 death path unavailable … (os error 13)` before its output. On the read-only path that
  the polkit rule and the plugin are designed to use, this is misleading noise.
- **N-F14-1 (parked by F14):** the startup evidence line cannot name the config source path because
  `RuntimePaths` has no such field.

## RULING F18 (frozen — implement exactly; `DESIGN.md` amended by the orchestrator)

- **A1 — expose degraded state (additive, schema ids unchanged):** `afanctl.state.v1` gains
  `"monitor_only": bool`; `afanctl.status.v1`'s `daemon` object gains `"monitor_only": bool`. Additive
  fields only — do not rename or remove anything, do not bump the `v1` ids (the plugin must keep
  working). Also make the cause visible: when the latch is set, `state.json`'s `recent_errors` already
  carries the fallback message — keep that behaviour and assert it in a test.
- **A2 — human `status` completeness (README is the contract):** print `mode` (with `monitor-only`
  clearly marked when the latch is set), `target` rpm when known, and a `recent_errors` line
  (`none` or the last error's timestamp + message). Keep the existing one-line-per-fact style and the
  exact machine-readable output untouched (`status --json` is the stable surface).
- **A3 — F15:** the smc discovery pre-open failure drops to `tracing::debug!` (keep the path/source
  fields). Rationale: callers that actually require the fd already report it themselves — `doctor`
  has its FAIL/PASS checks (F11) and `Supervisor::run()` logs its own error when a writing mode cannot
  be entered. Behavior (i.e. `panic_fd() == None`) must not change; no silent failure is introduced.
- **A4 — N-F14-1:** `RuntimePaths` gains `pub config_source: std::path::PathBuf`, populated by
  `cli.rs` from the resolved global `--config` value, and the startup evidence line logs it as
  `config_source=…`. This is an approved signature change: amend `DESIGN.md`'s Appendix A yourself
  (the orchestrator has already recorded the ruling; keep the change minimal and update the one
  existing test that constructs `RuntimePaths`).

## Files owned

`src/cli.rs`, `src/supervisor.rs`, `src/smc.rs` (A3 only — the log level, nothing else),
`tests/integration.rs`, `tests/schema.rs`, `README.md`. Everything else is read-only; do not touch
`main`, `src/policy.rs`, `src/doctor.rs`, `src/config.rs`.

## Tests required

1. `status --json` contains `daemon.monitor_only` and it is `false` in the healthy fixture daemon.
2. **Induced degradation on fixtures:** make the fixture's `fan1_output` unwritable (e.g. mode `0444`)
   for a spawned `daemon --mode curve` ⇒ the write fails ⇒ after `WRITE_FAIL_FALLBACK` the state file
   reports `monitor_only: true` with the fallback message in `recent_errors`, and human `status`
   marks the state clearly. (This is the F17b regression test — the exact condition that misled a
   reader during the hardware gate.)
3. Human `status` prints `mode`, `target` and a `recent_errors` line (assert on the rendered string,
   fixture-backed).
4. `tests/schema.rs`: `afanctl.state.v1` and `afanctl.status.v1` both validated with the new field
   present; existing fields unchanged.
5. `README.md` updated where it describes `status` output and the new `monitor_only` field (docs must
   match observed behavior).

## Constraints

- No new dependencies. No `unsafe`. No `unwrap`/`expect`/`panic!` outside tests.
- LOC: ≤ +150 product code (tests excluded). Over that = stop and report.
- Signature change (A4) is pre-approved; any *other* public-item change ⇒ `DEVIATIONS.md` + stop that part.
- Never write to real `/sys` in tests: fixtures only, via `--sysfs-root`.

## Done when (paste all four outputs in your summary)

```
cargo fmt --check
cargo clippy --all-targets --all-features -- -D warnings
cargo test
cargo test --features hw      # must SKIP cleanly
```

plus tests 1–5 pass; commit on the current branch; `git status` clean; do not merge; do not touch `main`.

## FINAL OUTPUT

Files changed · gate outputs · tests added · deviations/questions logged · anything left over.

---

## CONVENTIONS (verbatim from DESIGN.md §8 — binding)

> This is a byte-faithful copy of PRD §8 as of plan time. If PRD §8 and this copy ever diverge, the PRD wins and the orchestrator re-renders from the PRD.

**Language & toolchain**
- Rust edition 2021; rustc 1.98 (Arch `extra/rust`; install via `sudo pacman -S rust` — or `pkexec --disable-internal-agent pacman -S rust` on this box).
- No async. No threads beyond the main loop (single-threaded design; `Send` bound on `Smc` is for the trait's future, not for spawning).
- Dependencies: allowlist in R11 only. Adding one = stop, record in `DEVIATIONS.md`, await planner.

**Correctness discipline**
- No `unwrap`/`expect`/`panic!` outside tests, `main.rs` wiring, and `safety.rs`'s deliberate test panic. Clippy denies them (`clippy::unwrap_used`, `clippy::expect_used`, `clippy::panic` — allowlisted per-file where required).
- `unsafe` only in `safety.rs`, each block carrying a `// SAFETY:` comment naming the async-signal-safety argument.
- Every state-changing `smc` write goes through write-verify (R1). Never update logical state from an unverified write (the `mbpfan.c:410` class).
- Temps are `MilliC`, never bare `i32` across module boundaries. rpm are `u32`.

**Style**
- `cargo fmt` default config, no customization. `//!` module doc on every file stating its contract and invariants in ≤10 lines. Public items get doc comments. Safety-critical lines get `// SAFETY:` or `// INVARIANT:` comments.
- Errors: per-module `thiserror` enum; error messages name the path/key and the fix. Logging via `tracing` macros only — no `println!` outside `cli.rs` output formatting.
- Tests: unit tests co-located (`#[cfg(test)]`); cross-module tests in `tests/`. Every behavioral claim in this PRD that is testable on fixtures/mocks **must** have a test (see R10 list).

**Safety of the build process itself (non-negotiable)**
- **Never write to the real `/sys` during development.** All tests run against `tests/fixtures/sysfs/` via `--sysfs-root`. Real-hardware tests run only behind `--features hw` + `AFANCTL_HWTEST=1` + applesmc present, and only in the final supervised gate (§9). If a test would write real sysfs without those guards, that test is a bug.
- Never run the daemon against real hardware as part of ordinary development; the supervised gate does that, with the user present.

**Cross-agent protocol**
- You may only create/modify the files listed in your task card. Everything else is read-only.
- Signatures come from Appendix A (repo copy: `DESIGN.md`). If a required change is discovered: implement everything else, record the proposed change in `DEVIATIONS.md` (old → new → why → which tasks are affected), and flag it in your final summary. Never silently rename/add public items.
- Ambiguity or a blocking question → write it to `QUESTIONS.md`, continue with the unambiguous remainder, and say so in your summary. Do not invent spec.
- LOC budget per file in §7; >20% over = stop and report (scope smell), do not absorb silently.

**Quality gates (every task, before declaring done)**
```
cargo fmt --check
cargo clippy --all-targets --all-features -- -D warnings
cargo test
cargo test --features hw   # must SKIP cleanly (no applesmc in dev containers) — proves the guard works
```
Plus the task card's own checks. A task is done only when all gates pass in a clean checkout of its branch.
