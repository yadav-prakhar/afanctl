//! Schema tests (PRD R10, owned by T8): `status --json` output validated
//! against the versioned `afanctl.status.v1` schema (Appendix B).
//!
//! T0 skeleton: no functional tests yet; this file exists so the gate runs
//! and T8 fills it in place.

#[test]
fn skeleton_schema_placeholder() {
    // T8 owns: afanctl.status.v1 schema validation of real `status --json`
    // output, plus afanctl.cmd.v1 / afanctl.state.v1 file schemas.
}
