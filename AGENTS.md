## Agent skills

### Issue tracker

Issues are tracked in GitHub Issues (yadav-prakhar/afanctl), via the `gh` CLI. See `docs/agents/issue-tracker.md`.

### Triage labels

Default label vocabulary (needs-triage, needs-info, ready-for-agent, ready-for-human, wontfix). See `docs/agents/triage-labels.md`.

### Domain docs

Single-context layout (CONTEXT.md + docs/adr/ at repo root; neither exists yet). See `docs/agents/domain.md`.
